{
  "name": "chordpro-studio",
  "version": "1.6.0",
  "private": true,
  "workspaces": [
    "app",
    "tools/stitch-mcp"
  ],
  "scripts": {
    "dev": "npm run dev --workspace app",
    "build": "npm run build --workspace app",
    "regression": "node scripts/regression.js",
    "smoke": "node scripts/smoke.js",
    "preview": "npm run preview --workspace app",
    "tauri": "npm run tauri --workspace app --",
    "mcp:stitch": "node tools/stitch-mcp/src/server.mjs"
  }
}
